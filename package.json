{
  "name": "neurocart-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install --prefix ecommerce-backend && npm install --prefix ecommerce-frontend",
    "build:frontend": "npm run build --prefix ecommerce-frontend",
    "move-dist": "node scripts/move-dist.js",
    "build": "npm run install-all && npm run build:frontend && npm run move-dist",
    "vercel-build": "npm run install-all && npm run build:frontend && npm run move-dist",
    "start": "npm start --prefix ecommerce-backend",
    "dev:backend": "npm run dev --prefix ecommerce-backend",
    "dev:frontend": "npm run dev --prefix ecommerce-frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
